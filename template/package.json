{
  "name": "{%= name %}",
  "version": "{%= version %}",
  "description": "{%= description %}",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "{%= author %} <{%= email %}> ({%= website %})",
  "license": "{%= license %}",
  "bugs": {
    "url": "{%= repo %}/issues"
  },
  "homepage": "{%= repo %}",
  "repository": {
    "type": "git",
    "url": "{%= repo %}.git"
  },
  "private": {%= private %}
}